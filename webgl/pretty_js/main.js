/* =======
MAIN LOGIC
========== */

//---console.log('my vertex shader: '+vshader('name1'));
//---console.log('my fragment shader: '+fshader('name1'));

//what happens on window resize
function onWindowResize(glCanvasList){
  //get the first, if not only, canvas element
  var canvas = glCanvasList[0].canvas;

  //...
}

//what happens on page load
function onPageLoad(glCanvasList){
  //get the first, if not only, canvas element
  var canvas = glCanvasList[0].canvas;

  //...
}
